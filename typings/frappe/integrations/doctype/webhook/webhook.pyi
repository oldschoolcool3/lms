"""
This type stub file was generated by pyright.
"""

import requests
import frappe
from frappe.model.document import Document

WEBHOOK_SECRET_HEADER = ...
class Webhook(Document):
	_DOCTYPE_NAME = ...
	if TYPE_CHECKING:
		background_jobs_queue: DF.Autocomplete | None
		condition: DF.SmallText | None
		enable_security: DF.Check
		enabled: DF.Check
		is_dynamic_url: DF.Check
		request_method: DF.Literal[POST, PUT, DELETE]
		request_structure: DF.Literal["", "Form URL-Encoded", JSON]
		request_url: DF.SmallText
		timeout: DF.Int
		webhook_data: DF.Table[WebhookData]
		webhook_docevent: DF.Literal[after_insert, on_update, on_submit, on_cancel, on_trash, on_update_after_submit, on_change, workflow_transition,]
		webhook_doctype: DF.Link
		webhook_headers: DF.Table[WebhookHeader]
		webhook_json: DF.Code | None
		webhook_secret: DF.Password | None
		...
	def validate(self): # -> None:
		...
	
	def on_update(self): # -> None:
		...
	
	def execute_for_doc(self, doc: Document): # -> None:
		...
	
	def validate_docevent(self): # -> None:
		...
	
	def validate_condition(self): # -> None:
		...
	
	def validate_request_url(self): # -> None:
		...
	
	def validate_request_body(self): # -> None:
		...
	
	def validate_repeating_fields(self): # -> None:
		"""Error when Same Field is entered multiple times in webhook_data"""
		...
	
	def validate_secret(self): # -> None:
		...
	
	@frappe.whitelist()
	def preview_meets_condition(self, preview_document: str): # -> str:
		...
	
	@frappe.whitelist()
	def preview_request_body(self, preview_document: str): # -> str:
		...
	


def get_context(doc): # -> dict[str, Any]:
	...

def enqueue_webhook(doc, webhook) -> None:
	...

def log_request(webhook: str, doctype: str, docname: str, url: str, headers: dict, data: dict, res: requests.Response | None = ...): # -> None:
	...

def get_webhook_headers(doc, webhook, data=...): # -> dict[Any, Any]:
	...

def get_webhook_data(doc, webhook): # -> dict[Any, Any] | Any:
	...

@frappe.whitelist()
def get_all_queues(): # -> dict_keys[str, int]:
	"""Fetches all workers and returns a list of available queue names."""
	...

