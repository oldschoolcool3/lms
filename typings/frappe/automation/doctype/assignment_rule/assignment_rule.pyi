"""
This type stub file was generated by pyright.
"""

import frappe
from collections.abc import Iterable
from frappe.model.document import Document

class AssignmentRule(Document):
	_DOCTYPE_NAME = ...
	if TYPE_CHECKING:
		assign_condition: DF.Code
		assignment_days: DF.Table[AssignmentRuleDay]
		close_condition: DF.Code | None
		current_index: DF.Int
		description: DF.SmallText
		disabled: DF.Check
		document_type: DF.Link
		due_date_based_on: DF.Literal[None]
		field: DF.Literal[None]
		last_user: DF.Link | None
		priority: DF.Int
		rule: DF.Literal["Round Robin", "Load Balancing", "Based on Field", "Weighted Distribution"]
		unassign_condition: DF.Code | None
		users: DF.TableMultiSelect[AssignmentRuleUser]
		weighted_users: DF.Table[AssignmentRuleUser]
		...
	def validate(self): # -> None:
		...
	
	def clear_cache(self): # -> None:
		...
	
	def validate_document_types(self): # -> None:
		...
	
	def validate_assignment_days(self): # -> None:
		...
	
	def apply_unassign(self, doc, assignments): # -> bool | None:
		...
	
	def apply_assign(self, doc): # -> bool | None:
		...
	
	def do_assignment(self, doc): # -> bool:
		...
	
	def clear_assignment(self, doc): # -> bool | None:
		"""Clear assignments"""
		...
	
	def close_assignments(self, doc): # -> bool | None:
		"""Close assignments"""
		...
	
	def get_user(self, doc): # -> Link | None:
		"""
		Get the next user for assignment
		"""
		...
	
	def get_user_round_robin(self): # -> Link:
		"""
		Get next user based on round robin
		"""
		...
	
	def get_user_load_balancing(self): # -> Link | None:
		"""Assign to the user with least number of open assignments"""
		...
	
	def get_user_based_on_field(self, doc): # -> None:
		...
	
	def get_weighted_user(self): # -> Link | None:
		"""
		Assign to the user based on weights assigned to users
		Each rule maintains its own counter.
		"""
		...
	
	def safe_eval(self, fieldname, doc): # -> Any | Literal[False]:
		...
	
	def get_assignment_days(self): # -> list[Any]:
		...
	
	def is_rule_not_applicable_today(self): # -> list[Any] | bool:
		...
	


def get_assignments(doc) -> list[dict]:
	...

@frappe.whitelist()
def bulk_apply(doctype: str, docnames: str | list[str]): # -> None:
	...

def reopen_closed_assignment(doc): # -> bool:
	...

def apply(doc=..., method=..., doctype=..., name=...): # -> None:
	...

def update_due_date(doc, state=...): # -> None:
	"""Run on_update on every Document (via hooks.py)"""
	...

def get_assignment_rules() -> list[str]:
	...

def get_repeated(values: Iterable) -> list:
	...

