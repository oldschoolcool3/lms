"""
This type stub file was generated by pyright.
"""

import frappe
from typing import Protocol, runtime_checkable
from frappe.model.document import Document

@runtime_checkable
class LogType(Protocol):
	"""Interface requirement for doctypes that can be cleared using log settings."""
	@staticmethod
	def clear_old_logs(days: int) -> None:
		...
	


class LogSettings(Document):
	_DOCTYPE_NAME = ...
	if TYPE_CHECKING:
		logs_to_clear: DF.Table[LogsToClear]
		...
	def validate(self): # -> None:
		...
	
	def remove_unsupported_doctypes(self): # -> None:
		...
	
	def add_default_logtypes(self): # -> None:
		...
	
	def clear_logs(self): # -> None:
		"""
		Log settings can clear any log type that's registered to it and provides a method to delete old logs.

		Check `LogDoctype` above for interface that doctypes need to implement.
		"""
		...
	
	def register_doctype(self, doctype: str, days=...): # -> None:
		...
	


def run_log_clean_up(): # -> None:
	...

@frappe.whitelist()
def has_unseen_error_log(): # -> dict[str, Any] | None:
	...

@frappe.whitelist()
@frappe.validate_and_sanitize_search_inputs
def get_log_doctypes(doctype: str, txt: str, searchfield: str, start: int, page_len: int, filters: list): # -> list[tuple[Any]]:
	...

def clear_log_table(doctype, days=...): # -> None:
	"""If any logtype table grows too large then clearing it with DELETE query
	is not feasible in reasonable time. This command copies recent data to new
	table and replaces current table with new smaller table.

	ref: https://mariadb.com/kb/en/big-deletes/#deleting-more-than-half-a-table
	"""
	...

