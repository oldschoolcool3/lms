"""
This type stub file was generated by pyright.
"""

import typing
from datetime import datetime
from frappe.model import default_fields
from frappe.model.document import Document
from frappe.utils import cached_property
from frappe.core.doctype.doctype.doctype import DocType

"""
Load metadata (DocType) class

Example:

	meta = frappe.get_meta('User')
	if meta.has_field('first_name'):
		print("DocType" table has field "first_name")


"""
ListOrTuple = list | tuple
SerializableTypes = str | int | float | datetime
DEFAULT_FIELD_LABELS = ...
LARGE_TABLE_SIZE_THRESHOLD = ...
LARGE_TABLE_RECENCY_THRESHOLD = ...
def get_meta(doctype: str | DocType, cached: bool = ...) -> _Meta:
	"""Get metadata for a doctype.

	Args:
	    doctype: The doctype as a string object.
	    cached: Whether to use cached metadata (default: True).

	Returns:
	    Meta object for the given doctype.
	"""
	...

def clear_meta_cache(doctype: str = ...): # -> None:
	...

def load_meta(doctype): # -> Meta:
	...

def get_table_columns(doctype): # -> list[str]:
	...

def load_doctype_from_file(doctype): # -> Any:
	...

class Meta(Document):
	_metaclass = ...
	default_fields = ...
	special_doctypes = ...
	standard_set_once_fields = ...
	def __init__(self, doctype: str | DocType) -> None:
		...
	
	def load_from_db(self): # -> None:
		...
	
	def process(self): # -> None:
		...
	
	def as_dict(self, no_nulls=...): # -> _dict[Any, Any]:
		...
	
	def get_link_fields(self): # -> list[Any] | tuple[Any, ...] | Any | None:
		...
	
	def get_data_fields(self): # -> list[Any] | tuple[Any, ...] | Any | None:
		...
	
	def get_phone_fields(self): # -> list[Any] | tuple[Any, ...] | Any | None:
		...
	
	def get_dynamic_link_fields(self): # -> list[Any] | tuple[Any, ...] | Any | None:
		...
	
	def get_masked_fields(self): # -> list[Any] | Any:
		...
	
	def get_select_fields(self): # -> list[Any] | tuple[Any, ...] | Any | None:
		...
	
	def get_image_fields(self): # -> list[Any] | tuple[Any, ...] | Any | None:
		...
	
	def get_code_fields(self): # -> list[Any] | tuple[Any, ...] | Any | None:
		...
	
	def get_set_only_once_fields(self): # -> list[Any] | tuple[Any, ...] | Any | None:
		"""Return fields with `set_only_once` set"""
		...
	
	def get_table_fields(self, include_computed=...): # -> list[_dict[Any, str]] | list[Any] | tuple[Any, ...] | Any | None:
		...
	
	def get_global_search_fields(self): # -> list[Any] | tuple[Any, ...] | Any | None:
		"""Return list of fields with `in_global_search` set and `name` if set."""
		...
	
	def get_valid_columns(self) -> list[str]:
		...
	
	def get_valid_fields(self) -> list[str]:
		...
	
	def get_field(self, fieldname): # -> None:
		"""Return docfield from meta."""
		...
	
	def has_field(self, fieldname): # -> bool:
		"""Return True if fieldname exists."""
		...
	
	def get_label(self, fieldname): # -> str:
		"""Return label of the given fieldname."""
		...
	
	def get_options(self, fieldname):
		...
	
	def get_link_doctype(self, fieldname): # -> None:
		...
	
	def get_search_fields(self): # -> list[str | Any]:
		...
	
	def get_fields_to_fetch(self, link_fieldname=...): # -> list[Any]:
		"""Return a list of docfield objects for fields whose values
		are to be fetched and updated for a particular link field.

		These fields are of type Data, Link, Text, Readonly and their
		fetch_from property is set as `link_fieldname`.`source_fieldname`"""
		...
	
	def get_list_fields(self): # -> list[str]:
		...
	
	def get_custom_fields(self): # -> list[Any]:
		...
	
	def get_title_field(self): # -> Any | Literal['name', 'title']:
		"""Return the title field of this doctype,
		explict via `title_field`, or `title` or `name`"""
		...
	
	def get_translatable_fields(self): # -> list[Any]:
		"""Return all fields that are translation enabled"""
		...
	
	def is_translatable(self, fieldname): # -> None:
		"""Return true of false given a field"""
		...
	
	def get_workflow(self): # -> list[Any] | list[_dict[Any, Any]] | list[list[Any | None]] | dict[Any, Any] | _dict[Any, Any] | list[Any | None] | Any | None:
		...
	
	def get_naming_series_options(self) -> list[str]:
		"""Get list naming series options."""
		...
	
	def add_custom_fields(self): # -> None:
		...
	
	def apply_property_setters(self): # -> None:
		...
	
	def add_custom_links_and_actions(self): # -> None:
		...
	
	def check_if_large_table(self): # -> None:
		"""Apply some heuristics to detect large tables.

		UI code can use this information to adapt accordingly."""
		...
	
	def init_field_caches(self): # -> None:
		...
	
	def sort_fields(self): # -> None:
		"""
		Sort fields on the basis of following rules (priority descending):
		- `field_order` property setter
		- `insert_after` computed based on default order for standard fields
		- `insert_after` property for custom fields
		"""
		...
	
	def set_custom_permissions(self): # -> None:
		"""Reset `permissions` with Custom DocPerm if exists"""
		...
	
	def get_fieldnames_with_value(self, with_field_meta=..., with_virtual_fields=...): # -> list[Any]:
		...
	
	def get_fields_to_check_permissions(self, user_permission_doctypes): # -> list[Any] | tuple[Any, ...] | Any | None:
		...
	
	def get_high_permlevel_fields(self): # -> list[Any]:
		"""Build list of fields with high perm level and all the higher perm levels defined."""
		...
	
	@cached_property
	def high_permlevel_fields(self): # -> list[Any]:
		...
	
	def get_permitted_fieldnames(self, parenttype=..., *, user=..., permission_type=..., with_virtual_fields=...): # -> list[Any] | list[str | Any]:
		"""Build list of `fieldname` with read perm level and all the higher perm levels defined.

		Note: If permissions are not defined for DocType, return all the fields with value.
		"""
		...
	
	def get_permlevel_access(self, permission_type=..., parenttype=..., *, user=...): # -> list[Any]:
		...
	
	def get_permissions(self, parenttype=...): # -> Table | list[Any] | tuple[Any, ...] | Any | None:
		...
	
	def get_dashboard_data(self): # -> _dict[Any, Any]:
		"""Return dashboard setup related to this doctype.

		This method will return the `data` property in the `[doctype]_dashboard.py`
		file in the doctype's folder, along with any overrides or extensions
		implemented in other Frappe applications via hooks.
		"""
		...
	
	def add_doctype_links(self, data): # -> None:
		"""add `links` child table in standard link dashboard format"""
		...
	
	def get_row_template(self): # -> str | None:
		...
	
	def get_list_template(self): # -> str | None:
		...
	
	def get_web_template(self, suffix=...): # -> str | None:
		"""Return the relative path of the row template for this doctype."""
		...
	
	def is_nested_set(self): # -> bool:
		...
	


def get_parent_dt(dt): # -> list[Any] | list[_dict[Any, Any]] | list[list[Any | None]] | dict[Any, Any] | _dict[Any, Any] | list[Any | None] | Literal['']:
	...

def set_fieldname(field_id, fieldname): # -> None:
	...

def get_field_currency(df, doc=...): # -> Any | None:
	"""get currency based on DocField options and fieldvalue in doc"""
	...

def get_field_precision(df, doc=..., currency=...): # -> int:
	"""get precision based on DocField options and fieldvalue in doc"""
	...

def get_precision_from_currency_format(currency: str) -> int:
	"""Get precision from currency format string if applicable."""
	...

def get_default_df(fieldname): # -> _dict[Any, str] | None:
	...

def trim_tables(doctype=..., dry_run=..., quiet=...): # -> dict[Any, Any]:
	"""
	Removes database fields that don't exist in the doctype (json or custom field). This may be needed
	as maintenance since removing a field in a DocType doesn't automatically
	delete the db field.
	"""
	...

def trim_table(doctype, dry_run=...): # -> list[str]:
	...

CACHE_PROPERTIES = ...
if typing.TYPE_CHECKING:
	class _Meta(Meta, DocType):
		...
	
	
is_single = ...
