version: '3'

# Frontend (Vue 3 SPA under frontend/) tasks.

tasks:
  dev:frontend:
    # Serves THIS checkout's frontend with HMR, proxying to a running bench
    # (the shared Docker bench on :8000 by default). Open the printed port on the
    # lms.localhost host, e.g. http://lms.localhost:8080/lms. Parallel worktrees
    # auto-take 8081, 8082, … all proxying to the one bench. See
    # docs/002-development/how-to-guides/005-develop-in-a-worktree.md.
    desc: Start the Vite dev server (proxies to a running bench site)
    dir: frontend
    cmds:
      - yarn dev

  build:frontend:
    # Runs upstream `yarn build`; src/socket.js imports
    # ../../../../sites/common_site_config.json, which only resolves inside a
    # bench. Outside one, build with `bench build --app lms`.
    desc: "Build the SPA into lms/public/frontend (needs a bench)"
    cmds:
      - npx nx run frontend:build

  test:frontend:
    desc: Run Vitest unit tests
    cmds:
      - npx nx run frontend:test

  lint:frontend:
    desc: Prettier check on frontend/src
    cmds:
      - npx nx run frontend:lint

  format:frontend:
    desc: Prettier write on frontend/src
    dir: frontend
    cmds:
      - npx prettier --write src
