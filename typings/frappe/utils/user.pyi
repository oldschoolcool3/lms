"""
This type stub file was generated by pyright.
"""

from typing import TYPE_CHECKING
from frappe import _dict
from frappe.core.doctype.user.user import User

if TYPE_CHECKING:
	...
class UserPermissions:
	"""
	A user permission object can be accessed as `frappe.get_user()`
	"""
	def __init__(self, name=...) -> None:
		...
	
	def setup_user(self): # -> None:
		...
	
	def get_roles(self): # -> list[str] | list[Any] | Any | None:
		"""get list of roles"""
		...
	
	def build_doctype_map(self): # -> None:
		"""build map of special doctype properties"""
		...
	
	def build_perm_map(self): # -> None:
		"""build map of permissions at level 0"""
		...
	
	def build_permissions(self): # -> None:
		"""build lists of what the user can read / write / create
		quirks:
		        read_only => Not in Search
		        in_create => Not in create
		"""
		...
	
	def get_defaults(self): # -> _dict[Any, Any] | Any:
		...
	
	def get_can_read(self): # -> list[Any]:
		"""return list of doctypes that the user can read"""
		...
	
	def load_user(self): # -> list[Any] | list[_dict[Any, Any]] | list[list[Any | None]] | dict[Any, Any] | _dict[Any, Any] | list[Any | None] | None:
		...
	
	def get_all_reports(self): # -> Any:
		...
	


def get_user_fullname(user: str) -> str:
	...

def get_fullname_and_avatar(user: str) -> _dict:
	...

def get_system_managers(only_name: bool = ...) -> list[str]:
	"""Return all system manager's user details."""
	...

def add_role(user: str, role: str) -> None:
	...

def add_system_manager(email: str, first_name: str | None = ..., last_name: str | None = ..., send_welcome_email: bool = ..., password: str | None = ...) -> User:
	...

def get_enabled_system_users() -> list[dict]:
	...

def is_website_user(username: str | None = ...) -> str | None:
	...

def is_system_user(username: str | None = ...) -> str | None:
	...

def get_users() -> list[dict]:
	...

def get_users_with_role(role: str) -> list[str]:
	...

def is_portal_user(): # -> Literal[True] | None:
	...

def get_portal_roles(): # -> list[Any]:
	...

