"""
This type stub file was generated by pyright.
"""

import frappe
from frappe.model.document import Document
from .utils import *
from frappe.core.api.file import *

exclude_from_linked_with = ...
URL_PREFIXES = ...
FILE_ENCODING_OPTIONS = ...
class File(Document):
	_DOCTYPE_NAME = ...
	if TYPE_CHECKING:
		attached_to_doctype: DF.Link | None
		attached_to_field: DF.Data | None
		attached_to_name: DF.Data | None
		content_hash: DF.Data | None
		file_name: DF.Data | None
		file_size: DF.Int
		file_type: DF.Data | None
		file_url: DF.Code | None
		folder: DF.Link | None
		is_attachments_folder: DF.Check
		is_folder: DF.Check
		is_home_folder: DF.Check
		is_private: DF.Check
		old_parent: DF.Data | None
		thumbnail_url: DF.SmallText | None
		uploaded_to_dropbox: DF.Check
		uploaded_to_google_drive: DF.Check
		...
	no_feed_on_delete = ...
	def __init__(self, *args, **kwargs) -> None:
		...
	
	@property
	def is_remote_file(self): # -> bool:
		...
	
	def autoname(self): # -> None:
		"""Set name for folder"""
		...
	
	def before_insert(self): # -> None:
		...
	
	def after_insert(self): # -> None:
		...
	
	def create_attachment_copy(self, attached_to_doctype: str, attached_to_name: str, attached_to_field: str | None = ..., ignore_permissions: bool = ...): # -> Document:
		"""Efficiently copy an attachment from one document to another by reusing `file_url`."""
		...
	
	def validate(self): # -> None:
		...
	
	def validate_attachment_references(self): # -> None:
		...
	
	def enforce_public_file_restrictions(self): # -> None:
		...
	
	def validate_private_file_access(self): # -> None:
		"""Validate that the user has permission to access an existing private file."""
		...
	
	def after_rename(self, *args, **kwargs): # -> None:
		...
	
	def on_trash(self): # -> None:
		...
	
	def on_rollback(self): # -> None:
		...
	
	def get_name_based_on_parent_folder(self) -> str | None:
		...
	
	def get_successors(self): # -> list[Any]:
		...
	
	def validate_file_path(self): # -> None:
		...
	
	def validate_file_url(self): # -> None:
		...
	
	def handle_is_private_changed(self): # -> None:
		...
	
	def fetch_attached_to_field(self, old_file_url): # -> Literal[True] | None:
		...
	
	def validate_attachment_limit(self): # -> None:
		...
	
	def validate_remote_file(self): # -> None:
		"""Validates if file uploaded using URL already exist"""
		...
	
	def set_folder_name(self): # -> None:
		"""Make parent folders if not exists based on reference doctype and name"""
		...
	
	def set_file_type(self): # -> None:
		...
	
	def validate_file_on_disk(self): # -> Literal[True] | None:
		"""Validates existence file"""
		...
	
	def validate_file_extension(self): # -> None:
		...
	
	def check_content(self): # -> None:
		...
	
	def validate_duplicate_entry(self): # -> None:
		...
	
	def set_file_name(self): # -> None:
		...
	
	def generate_content_hash(self): # -> None:
		...
	
	def make_thumbnail(self, set_as_thumbnail: bool = ..., width: int = ..., height: int = ..., suffix: str = ..., crop: bool = ...) -> str:
		...
	
	def validate_empty_folder(self): # -> None:
		"""Throw exception if folder is not empty"""
		...
	
	def validate_protected_file(self): # -> None:
		"""Throw an exception if this file is attached to a doctype that protects files.

		Allows deleting the attached file if the linked document is in draft. If submitted,
		deletion is not allowed. If canceled, requires delete permissions on the linked document.
		"""
		...
	
	def unzip(self) -> list[File]:
		"""Unzip current file and replace it by its children"""
		...
	
	def exists_on_disk(self): # -> bool:
		...
	
	def get_content(self, encodings=...) -> bytes | str:
		...
	
	def get_full_path(self): # -> str | None:
		"""Return file path using the set file name."""
		...
	
	def write_file(self): # -> str | None:
		"""write file to disk with a random name (to compare)"""
		...
	
	def save_file(self, content: bytes | str | None = ..., decode=..., ignore_existing_file_check=..., overwrite=...): # -> Any | dict[str, Any] | None:
		...
	
	def save_file_on_filesystem(self): # -> dict[str, Any]:
		...
	
	def check_max_file_size(self): # -> int:
		...
	
	def delete_file_data_content(self, only_thumbnail=...): # -> None:
		...
	
	def delete_file_from_filesystem(self, only_thumbnail=...): # -> None:
		"""Delete file, thumbnail from File document"""
		...
	
	def is_downloadable(self): # -> bool:
		...
	
	def get_extension(self):
		"""Split and return filename and extension for the set `file_name`."""
		...
	
	def create_attachment_record(self): # -> None:
		...
	
	def add_comment_in_reference_doc(self, comment_type, text): # -> None:
		...
	
	def set_is_private(self): # -> None:
		...
	
	@frappe.whitelist()
	def optimize_file(self): # -> None:
		...
	
	@property
	def unique_url(self) -> str:
		"""Unique URL contains file ID in URL to speed up permisison checks."""
		...
	
	@staticmethod
	def zip_files(files): # -> bytes:
		...
	


def on_doctype_update(): # -> None:
	...

def has_permission(doc, ptype=..., user=..., debug=...): # -> bool:
	...

def get_permission_query_conditions(user: str | None = ...) -> str:
	...

